# Local config
[[ -f ~/.zshrc.local ]] && source ~/.zshrc.local

# aliases
[[ -f ~/.aliases ]] && source ~/.aliases

# Created by `pipx` on 2024-05-14 09:16:00
export PATH="$PATH:/Users/darkrai/.local/bin"
